[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs", "python3", "python3Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r requirements.txt --break-system-packages",
  "python3 --version"
]
